# "c" for c(++)-side of things
cp platform/droid/* android/jni &&
cp -R common android/jni &&
rsync -av bgfx android/jni --exclude .git &&
rsync -av bx android/jni --exclude .git &&
cd android &&
ndk-build -j8 &&
cd ..
