#pragma once

#include <string>

namespace noob
{
	// This allows you to define fast-drawing, static text.
	struct text_label
	{
	// noob::textbuffer_handle;
		float x, y;
		// noob::gui::font_size;
		uint32_t colour;
	};
}
