# "b" for "build"
cd ../engine/build &&
cmake -DNOOB_PLATFORM_LINUX:bool=true .. && #-DNOOB_USE_ASSIMP:bool=true .. &&
make -j8 2> output && 
#cp output ../../sandbox &&
cp NoobEdit ../../sandbox/e &&
cp output ../../sandbox &&
cd ../../sandbox #&&
#strip ./e
